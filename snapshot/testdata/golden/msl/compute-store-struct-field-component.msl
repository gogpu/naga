// language: metal1.0
#include <metal_stdlib>
#include <simd/simd.h>

using metal::uint;
struct DefaultConstructible {
    template<typename T>
    operator T() && {
        return T {};
    }
};

struct _mslBufferSizes {
    uint size0;
};

struct Particle {
    metal::float2 pos;
    metal::float2 vel;
};
typedef Particle type_1[1];

kernel void main_(
  device type_1& out [[user(fake0)]]
, constant _mslBufferSizes& _buffer_sizes [[user(fake0)]]
) {
    Particle p = {};
    p.pos = metal::float2(0.0, 0.0);
    p.vel = metal::float2(0.0, 0.0);
    p.pos.x = 1.0;
    p.pos.y = 2.0;
    p.vel.x = 3.0;
    p.vel.y = 4.0;
    Particle _e23 = p;
    if (uint(0) < 1 + (_buffer_sizes.size0 - 0 - 16) / 16) {
        out[0] = _e23;
    }
    return;
}
