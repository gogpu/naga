// language: metal1.0
#include <metal_stdlib>
#include <simd/simd.h>

using metal::uint;
struct DefaultConstructible {
    template<typename T>
    operator T() && {
        return T {};
    }
};

struct type_1 {
    int inner[2];
};

kernel void main_(
  device type_1& out [[user(fake0)]]
) {
    type_1 tmp = type_1 {1, 2};
    int i = 0;
    uint2 loop_bound = uint2(4294967295u);
    bool loop_init = true;
    while(true) {
        if (metal::all(loop_bound == uint2(0u))) { break; }
        loop_bound -= uint2(loop_bound.y == 0u, 1u);
        if (!loop_init) {
            int _e16 = i;
            i = as_type<int>(as_type<uint>(_e16) + as_type<uint>(1));
        }
        loop_init = false;
        int _e6 = i;
        if (_e6 < 2) {
        } else {
            break;
        }
        {
            int _e10 = i;
            int _e12 = i;
            int _e14 = uint(_e12) < 2 ? tmp.inner[_e12] : DefaultConstructible();
            if (uint(_e10) < 2) {
                out.inner[_e10] = _e14;
            }
        }
    }
    return;
}
